#使用了iter的函数迭代器
list=[1,2,3,4]
it = iter(list)    # 创建迭代器对象
for x in it:
    print (x, end=" ")

#使用StopIteration类似于break停止循环
if self.a <= 20:
    x = self.a
    self.a += 1
    return x
else:
      raise StopIteration

#使用了yield的函数称为生成器


